var num = parseFloat(prompt("Entre com um número real:"));

if (num >= 0 && num <= 10){
	alert("O número está no intervalo de 0 a 10");
	end;
	if (num > 30 && num < 40){
		alert("O número esta no intervalo de 30 a 40");
		end;
	}
}
else{
	alert("O número está fora dos intervalos")
}
